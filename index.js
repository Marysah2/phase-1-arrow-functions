// 1. Declare a function using a function expression
const divide = function(a, b) {
  return a / b;
};

// 2. Declare an arrow function with two parameters
const square = (x) => x * x;

// 3. Declare an arrow function with one parameter
const add = (a, b) => a + b;
